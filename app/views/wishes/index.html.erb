<div class="container">
  <% @wishes.each do |wish| %>
    <%= link_to wish_path(wish.id), class: "sock" do %>
      <div>
        <h2><%= wish.context %></h3>
        <h3 class="writer"><%= wish.writer %></h2>
      </div>
    <% end %>
  <% end %>
</div>

<%= link_to "新規作成", new_wish_path %>
